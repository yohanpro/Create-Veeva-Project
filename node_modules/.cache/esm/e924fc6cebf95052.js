let arg,inquirer,chalk,createProject;_ebf‍.x([["cli",()=>cli]]);_ebf‍.w("arg",[["default",["arg"],function(v){arg=v}]]);_ebf‍.w("inquirer",[["default",["inquirer"],function(v){inquirer=v}]]);_ebf‍.w("chalk",[["default",["chalk"],function(v){chalk=v}]]);_ebf‍.w("./main",[["createProject",["createProject"],function(v){createProject=v}]]);






function parseArgumentsIntoOptions(rawArgs) {
  const args = arg({
    "--git": Boolean,
    "--install": Boolean,
    "-i": "--install"
  }, {
    argv: rawArgs.slice(2)
  });
  return {
    git: args["--git"] || false,
    template: args._[0],
    runInstall: args["--install"] || false
  };
}
async function promptForMissingOptions(options) {
  const questions = [];

  questions.push({
    type: "input",
    name: "presentation",
    message: "Name of Veeva root Presentation"
  });
  if (!options.git) {
    questions.push({
      type: "confirm",
      name: "git",
      message: "Initalize a git repository?",
      default: false
    });
  }
  questions.push({
    type: "number",
    name: "slide",
    message: "Total number of Slide?",
    validate: function (value) {
      var valid = !isNaN(parseFloat(value));
      if (!valid) {
        _ebf‍.g.console.log("\n %s Please enter number", chalk.redBright.bold("ERROR"))
        process.exit(1);
      } else {
        return true
      }
    }
  });
  questions.push({
    type: "input",
    name: "product",
    message: "Name of Veeva Product"
  });
  questions.push({
    type: "confirm",
    name: "seperate",
    message: "Do you want to seperate Main and Add?",
    default: false
  });

  const answers = await inquirer.prompt(questions);

  return {
    ...options,
    git: options.git || answers.git,
    template: options.template || answers.template,
    presentation: options.presentation || answers.presentation,
    ...answers
  };
}
       async function cli(args) {
  let options = parseArgumentsIntoOptions(args);
  options = await promptForMissingOptions(options);
  await createProject(options);
  _ebf‍.g.console.log(options);
}