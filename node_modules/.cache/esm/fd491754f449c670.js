let chalk,fs,DIRECTORIES,shell,path;_f44‍.x([["makeSlides",()=>makeSlides]]);_f44‍.w("chalk",[["default",["chalk"],function(v){chalk=v}]]);_f44‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_f44‍.w("../main",[["DIRECTORIES",["DIRECTORIES"],function(v){DIRECTORIES=v}]]);_f44‍.w("shelljs",[["default",["shell"],function(v){shell=v}]]);_f44‍.w("path",[["default",["path"],function(v){path=v}]]);








/*
 1. options에 있는 슬라이드 개수를 가져온다.
2. main과 Add가 나누어져 있는지 확인한다.
3. main과 ADD가 나누어져 있을 경우 
 - 
4. 나누어져 있지 않을 경우 
- 
5. 
6. 슬라이드를 만들어준다.개수에 맞게끔..
*/
       const makeSlides = async (options) => {
    if (options.seperate) {
        DIRECTORIES = {
            ...DIRECTORIES,
            pres_MAIN_Dir: _f44‍.a("path",path).join(_f44‍.a("DIRECTORIES",DIRECTORIES).presentationDir, `${options.presentation}_MAIN`),
            pres_ADD_Dir: _f44‍.a("path",path).join(_f44‍.a("DIRECTORIES",DIRECTORIES).presentationDir, `${options.presentation}_ADD`)
        }
    }
    await makeRefAndPI(options);
    await makeSlidesFolders(options);
};;_f44‍.j(["makeSlides"]);


const makeRefAndPI = async options => {

    if (options.seperate) {
        _f44‍.a("shell",shell).cd(_f44‍.a("DIRECTORIES",DIRECTORIES).pres_ADD_Dir);
    } else {
        _f44‍.a("shell",shell).cd(_f44‍.a("DIRECTORIES",DIRECTORIES).presentationDir);
    }
    _f44‍.a("shell",shell).mkdir([`${options.presentation}_REFS`, `${options.presentation}_PI`])
    _f44‍.a("shell",shell).cd('..');
    return;
}

const makeSlidesFolders = async (options) => {
    const numOfSlideCount = options.slide;
    if (options.seperate) {
        _f44‍.a("shell",shell).cd(_f44‍.a("DIRECTORIES",DIRECTORIES).pres_MAIN_Dir);
    } else {
        _f44‍.a("shell",shell).cd(_f44‍.a("DIRECTORIES",DIRECTORIES).presentationDir);
    }
    for (let i = 0; i < numOfSlideCount; i++) {
        let name = "";
        if (i < 10) {
            name = "00" + i;
        } else if (i >= 10) {
            name = "0" + i;
        }
        _f44‍.a("shell",shell).mkdir(`${options.presentation}_${name}`);
    }
}